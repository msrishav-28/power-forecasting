# Local backend URL.
# In production set to your Render service URL, e.g. https://powergrid-eri-api.onrender.com
# Note: Vite bakes this value at build time, so update it in the Vercel dashboard and redeploy after changes.
VITE_API_BASE_URL=http://localhost:8000
